import { Card, CardContent, Grid, Typography } from '@mui/material';
import { useAuthStore } from '../store/authStore';
export function DashboardPage() { const user = useAuthStore((state) => state.user); return <><Typography variant="h4" component="h1" gutterBottom>Welcome, {user?.firstName}</Typography><Typography color="text.secondary" sx={{ mb: 3 }}>Your secured TransitOps workspace is ready.</Typography><Grid container spacing={2}><Grid size={{ xs: 12, md: 6 }}><Card><CardContent><Typography variant="h6">Authentication</Typography><Typography color="success.main">Session active</Typography></CardContent></Card></Grid><Grid size={{ xs: 12, md: 6 }}><Card><CardContent><Typography variant="h6">Access level</Typography><Typography>{user?.role.description ?? user?.role.name}</Typography></CardContent></Card></Grid></Grid></>; }
